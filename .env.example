# OTS Hub — Environment Variables

HOST=0.0.0.0
PORT=8000
DEBUG=False

# Token compartilhado entre Hub e todos os processos
ORACLE_TOKEN=gerar-um-token-seguro-aqui

ALLOWED_ORIGINS=*

# Supabase (opcional — deixe vazio para desabilitar persistência)
SUPABASE_URL=
SUPABASE_KEY=
