# OTS Hub — Dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
websockets>=11.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Database (optional — set SUPABASE_URL/KEY in .env)
supabase>=2.0.0

# Dev/Test
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.23.0
